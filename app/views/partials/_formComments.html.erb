<%= form_with url: "/posts/#{@post.id}/comments", scope: :comment do |form| %>
<div class="comment-from">
	<%= form.text_area :text, placeholder: "Type something here...", required: true %>
	<%= form.submit "Comment", class: "submit-btn comment-btn" %>
  </div>
<% end %>